<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistem Irigare Automatizată | Bare-Metal AVR</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@300;400;700&family=Share+Tech+Mono&display=swap" rel="stylesheet">

    <style>
        /* ================= VARIABLES ================= */
        :root {
            --bg-color: #0a0a0c;
            --surface-color: #161b22;
            --border-color: #30363d;
            --text-main: #c9d1d9;
            --text-muted: #8b949e;
            --accent-green: #00ff66;
            --accent-amber: #ffb000;
            --accent-blue: #58a6ff;
            --font-display: 'Share Tech Mono', monospace;
            --font-code: 'JetBrains Mono', monospace;
        }

        /* ================= SCROLLBAR CUSTOM ================= */
        ::-webkit-scrollbar { width: 10px; }
        ::-webkit-scrollbar-track { background: var(--bg-color); }
        ::-webkit-scrollbar-thumb { 
            background: var(--surface-color); 
            border: 1px solid var(--accent-green); 
            border-radius: 2px;
        }
        ::-webkit-scrollbar-thumb:hover { background: rgba(0, 255, 102, 0.2); }

        /* ================= RESET & BASE ================= */
        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            font-family: var(--font-code);
            line-height: 1.6;
            overflow-x: hidden;
            background-image: 
                linear-gradient(rgba(0, 255, 102, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 255, 102, 0.03) 1px, transparent 1px);
            background-size: 30px 30px;
        }

        h1, h2, h3 {
            font-family: var(--font-display);
            font-weight: 400;
            color: var(--accent-green);
        }

        a { text-decoration: none; color: var(--accent-green); transition: 0.3s; }
        a:hover { color: var(--accent-amber); text-shadow: 0 0 8px rgba(255,176,0,0.5); }

        .container { 
            max-width: 1000px; margin: 0 auto; padding: 0 20px; 
            position: relative; z-index: 1; 
        }

        /* ================= PARTICLES BACKGROUND ================= */
        #particles-js {
            position: fixed; width: 100vw; height: 100vh;
            top: 0; left: 0; z-index: 0; pointer-events: none; 
        }

        /* ================= TOOLTIPS ================= */
        .tech-word {
            position: relative;
            cursor: help;
            color: var(--accent-green);
            border-bottom: 1px dashed rgba(0, 255, 102, 0.5);
            transition: 0.3s;
        }

        .tech-word:hover { text-shadow: 0 0 8px rgba(0, 255, 102, 0.5); }

        .tech-word::after {
            content: attr(data-tooltip);
            position: absolute; bottom: 130%; left: 50%; transform: translateX(-50%);
            background: #0d1117; border: 1px solid var(--accent-green);
            color: var(--text-main); padding: 8px 12px; border-radius: 4px;
            font-size: 0.85rem; width: max-content; max-width: 250px;
            white-space: normal; opacity: 0; visibility: hidden; transition: 0.3s ease;
            box-shadow: 0 0 15px rgba(0, 255, 102, 0.1); z-index: 100;
            pointer-events: none; line-height: 1.4;
        }

        .tech-word::before {
            content: ''; position: absolute; bottom: 100%; left: 50%;
            transform: translateX(-50%); border-width: 6px; border-style: solid;
            border-color: var(--accent-green) transparent transparent transparent;
            opacity: 0; visibility: hidden; transition: 0.3s ease;
            z-index: 100; pointer-events: none;
        }

        .tech-word:hover::after, .tech-word:hover::before {
            opacity: 1; visibility: visible;
        }

        /* ================= ANIMATIONS & EFFECTS ================= */
        .reveal {
            opacity: 0; transform: translateY(30px);
            transition: all 0.8s cubic-bezier(0.5, 0, 0, 1);
        }
        .reveal.active { opacity: 1; transform: translateY(0); }

        @keyframes blinkCursor { 50% { border-right-color: transparent; } }
        
        @keyframes scanline {
            0% { transform: translateY(-100%); }
            100% { transform: translateY(100vh); }
        }

        @keyframes glitch {
            0% { text-shadow: 2px 0 var(--accent-amber), -2px 0 var(--accent-green); }
            50% { text-shadow: -2px 0 var(--accent-amber), 2px 0 var(--accent-green); }
            100% { text-shadow: 2px 0 var(--accent-amber), -2px 0 var(--accent-green); }
        }

        @keyframes spin { 100% { transform: rotate(360deg); } }
        
        @keyframes drip {
            0% { transform: translateY(-5px); opacity: 0; }
            50% { opacity: 1; }
            100% { transform: translateY(20px); opacity: 0; }
        }

        .scanline {
            position: fixed; top: 0; left: 0; width: 100%; height: 10px;
            background: rgba(0, 255, 102, 0.1); opacity: 0.4;
            pointer-events: none; animation: scanline 8s linear infinite; z-index: 9999;
        }

        /* ================= ANIMATED PUMP WIDGET ================= */
        .pump-widget {
            position: fixed; bottom: 30px; right: 30px;
            background: rgba(13, 17, 23, 0.85); border: 1px solid var(--border-color);
            border-radius: 8px; padding: 12px 20px; display: flex; align-items: center; gap: 15px;
            backdrop-filter: blur(8px); z-index: 1000; box-shadow: 0 4px 15px rgba(0,0,0,0.5);
            transition: 0.3s;
        }
        
        .pump-widget:hover { border-color: var(--accent-blue); }
        
        .pump-icon {
            width: 36px; height: 36px; border: 2px solid var(--text-muted);
            border-radius: 50%; display: flex; justify-content: center; align-items: center;
            position: relative; background: #161b22;
        }
        
        .impeller {
            width: 18px; height: 18px; border: 2px dashed var(--accent-blue);
            border-radius: 50%; animation: spin 1.5s linear infinite;
        }
        
        .water-drop {
            width: 6px; height: 10px; background: var(--accent-blue); 
            border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%;
            position: absolute; bottom: -18px; animation: drip 1.2s infinite ease-in;
        }
        
        .pump-info { display: flex; flex-direction: column; }
        .pump-title { font-family: var(--font-display); font-size: 0.85rem; color: var(--text-muted); text-transform: uppercase;}
        .pump-status { font-weight: bold; color: var(--accent-blue); font-size: 0.95rem; display: flex; align-items: center; gap: 5px;}
        .status-dot { width: 8px; height: 8px; background: var(--accent-blue); border-radius: 50%; box-shadow: 0 0 8px var(--accent-blue); }

        /* ================= HERO SECTION ================= */
        .hero {
            min-height: 90vh; display: flex; flex-direction: column;
            justify-content: center; border-bottom: 1px solid var(--border-color);
            position: relative;
        }

        .terminal-header { color: var(--text-muted); margin-bottom: 20px; font-size: 0.9rem; }

        .hero h1 {
            font-size: clamp(2rem, 5vw, 4rem); margin-bottom: 20px;
            text-shadow: 0 0 10px rgba(0, 255, 102, 0.3);
            border-right: 3px solid var(--accent-green);
            white-space: nowrap; overflow: hidden; width: fit-content;
            animation: blinkCursor 0.8s step-end infinite;
        }

        .hero p { font-size: 1.2rem; color: var(--text-muted); max-width: 700px; margin-bottom: 40px; }

        .btn {
            display: inline-block; padding: 12px 24px;
            border: 1px solid var(--accent-green); background: rgba(10, 10, 12, 0.8);
            color: var(--accent-green); font-family: var(--font-display);
            font-size: 1.1rem; text-transform: uppercase; cursor: pointer;
            transition: all 0.3s ease; position: relative; overflow: hidden;
            backdrop-filter: blur(4px);
        }

        .btn:hover {
            background: rgba(0, 255, 102, 0.1);
            box-shadow: 0 0 20px rgba(0, 255, 102, 0.4);
            animation: glitch 0.2s linear infinite;
        }

        /* ================= SECTIONS ================= */
        section { padding: 80px 0; border-bottom: 1px solid var(--border-color); }
        .section-title { font-size: 2rem; margin-bottom: 40px; position: relative; display: inline-block;}
        .section-title::after {
            content: ''; position: absolute; bottom: -5px; left: 0;
            width: 50%; height: 2px; background: var(--accent-green);
        }
        .section-title:hover { animation: glitch 0.3s linear infinite; cursor: crosshair; }

        /* ================= GRID SPECS ================= */
        .specs-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; }
        .spec-item {
            background: rgba(22, 27, 34, 0.85); padding: 20px; backdrop-filter: blur(5px);
            border-left: 3px solid var(--border-color); transition: 0.3s;
        }
        .spec-item:hover { border-left-color: var(--accent-amber); transform: translateX(5px); }
        .spec-item h3 { font-size: 1.2rem; margin-bottom: 10px; color: var(--text-main); }
        .spec-item p { color: var(--text-muted); font-size: 0.9rem; }

        /* ================= FLORA DATABASE (FĂRĂ IMAGINI) ================= */
        .flora-grid {
            display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px; margin-top: 20px;
        }
        .flora-card {
            background: rgba(22, 27, 34, 0.85); border: 1px solid var(--border-color);
            border-top: 3px solid var(--accent-green); /* Înlocuiește vizual spațiul imaginii */
            border-radius: 4px; overflow: hidden; transition: all 0.3s ease; backdrop-filter: blur(5px);
        }
        .flora-card:hover { border-color: var(--accent-amber); box-shadow: 0 0 15px rgba(255, 176, 0, 0.15); }
        .flora-details { padding: 25px; }
        .flora-details h3 {
            color: var(--text-main); font-size: 1.4rem; margin-bottom: 20px;
            display: flex; justify-content: space-between; align-items: center;
        }
        .badge {
            font-size: 0.75rem; padding: 3px 8px; background: rgba(0, 255, 102, 0.1);
            color: var(--accent-green); border: 1px solid var(--accent-green);
            border-radius: 12px; font-family: var(--font-code);
        }
        .param-row {
            display: flex; justify-content: space-between; align-items: center;
            margin-bottom: 12px; font-size: 0.85rem; border-bottom: 1px dotted #30363d; padding-bottom: 6px;
        }
        .param-label { color: var(--text-muted); }
        .param-value { color: var(--accent-amber); font-weight: bold; letter-spacing: 1px; }

        /* ================= ARCHITECTURE & DRIVERS ================= */
        .arch-container {
            display: flex; align-items: center; justify-content: space-between;
            background: rgba(22, 27, 34, 0.85); padding: 40px; border: 1px solid var(--border-color);
            border-radius: 4px; flex-wrap: wrap; gap: 20px; backdrop-filter: blur(5px);
        }
        .node {
            flex: 1; min-width: 200px; text-align: center; padding: 30px;
            border: 1px dashed var(--accent-green); background: rgba(0,255,102,0.05);
        }
        .connection {
            display: flex; flex-direction: column; align-items: center;
            color: var(--accent-amber); font-family: var(--font-display);
        }
        .arrow { font-size: 2rem; margin: -10px 0; }

        /* ================= BILL OF MATERIALS (BOM) ================= */
        .bom-table {
            width: 100%; border-collapse: collapse; margin-top: 20px;
            background: rgba(22, 27, 34, 0.85); border: 1px solid var(--border-color);
            backdrop-filter: blur(5px);
        }
        .bom-table th, .bom-table td {
            padding: 12px 15px; text-align: left; border-bottom: 1px solid var(--border-color);
        }
        .bom-table th { color: var(--accent-green); font-family: var(--font-display); font-size: 1.1rem; }
        .bom-table tr:hover td { background: rgba(0,255,102,0.05); color: var(--accent-amber); }
        .bom-table td:last-child { color: var(--text-muted); font-size: 0.9rem; }

        /* ================= ROADMAP ================= */
        .roadmap-list { list-style: none; padding: 0; }
        .roadmap-list li { 
            margin-bottom: 15px; padding: 15px; background: rgba(22, 27, 34, 0.85); 
            border-left: 2px solid #30363d; backdrop-filter: blur(5px);
        }
        .roadmap-list li.done { border-left-color: var(--accent-green); }
        .roadmap-list li.pending { border-left-color: var(--accent-amber); }
        .status { font-weight: bold; margin-right: 10px; }
        .done .status { color: var(--accent-green); }
        .pending .status { color: var(--accent-amber); }

        /* ================= TERMINAL ================= */
        .terminal-window {
            background: #0d1117; border: 1px solid var(--border-color);
            border-radius: 6px; overflow: hidden; margin-top: 20px;
        }
        .terminal-bar {
            background: #161b22; border-bottom: 1px solid var(--border-color);
            padding: 10px 15px; display: flex; gap: 8px;
        }
        .dot { width: 12px; height: 12px; border-radius: 50%; }
        .dot.r { background: #ff5f56; } .dot.y { background: #ffbd2e; } .dot.g { background: #27c93f; }
        .terminal-body { padding: 20px; overflow-x: auto; }
        .terminal-body pre { color: var(--text-main); font-size: 0.9rem; }
        .prompt { color: var(--accent-green); }
        .comment { color: #8b949e; }

        /* ================= FOOTER ================= */
        footer { text-align: center; padding: 60px 0; }
        
        /* ================= RESPONSIVE ================= */
        @media (max-width: 768px) {
            .hero h1 { animation: none; white-space: normal; border-right: none; }
            .arch-container { flex-direction: column; }
            .connection { transform: rotate(90deg); margin: 20px 0; }
            .param-row { flex-direction: column; align-items: flex-start; }
            .bom-table { display: block; overflow-x: auto; }
            .tech-word::after { max-width: 200px; font-size: 0.75rem; }
            .pump-widget { bottom: 15px; right: 15px; padding: 10px; }
            .pump-title { display: none; }
        }
    </style>
</head>
<body>

    <div id="particles-js"></div>
    <div class="scanline"></div>

    <aside class="pump-widget">
        <div class="pump-icon">
            <div class="impeller"></div>
            <div class="water-drop"></div>
        </div>
        <div class="pump-info">
            <span class="pump-title">Canal Irigare M1</span>
            <span class="pump-status"><span class="status-dot"></span> POMPĂ ACTIVĂ</span>
        </div>
    </aside>

    <main class="container">
        <header class="hero reveal">
            <div class="terminal-header">victor@ubuntu:~/projects/smart-irrigation$ ./run_presentation.sh</div>
            <h1 id="typewriter">Sistem_Irigare_Automatizată</h1>
            <p>Arhitectură Master-Slave. Implementare integrală în C <span class="tech-word" data-tooltip="Programare la nivel de regiștri hardware, fără sistem de operare sau framework-uri specifice precum Arduino IDE.">bare-metal</span>. Manipulare directă a regiștrilor AVR.</p>
            <div>
                <a href="https://github.com/razvanstefandima-11/Sistem-Automat-de-Irigare-pentru-Plante" target="_blank" class="btn">View Source pe GitHub_</a>
            </div>
        </header>

        <section class="reveal">
            <h2 class="section-title">Hardware_Specs</h2>
            <div class="specs-grid">
                <div class="spec-item">
                    <h3>MCU Principal</h3>
                    <p>Arduino Uno (ATmega328P). Rol de Master. Gestionează logica decizională, interfața <span class="tech-word" data-tooltip="Inter-Integrated Circuit. Protocol de comunicație serială sincronă pe 2 fire (SDA și SCL).">I2C</span> și senzorul ultrasonic.</p>
                </div>
                <div class="spec-item">
                    <h3>MCU Secundar</h3>
                    <p>Arduino Nano. Rol de Slave. Achiziție date senzori (<span class="tech-word" data-tooltip="Analog-to-Digital Converter. Modulul HW care convertește tensiunea analogică în valori digitale (0-1023).">ADC</span>) și acționare elemente de execuție (GPIO/Pompe).</p>
                </div>
            </div>
        </section>

        <section class="reveal">
            <h2 class="section-title">Arhitectură_Sistem</h2>
            <div class="arch-container">
                <div class="node">
                    <h3>MASTER (Uno)</h3>
                    <p style="color:var(--text-muted); font-size:0.9em; margin-top:10px;">
                        [Logic Control]<br>Display LCD (I2C)<br>Ultrasonic (<span class="tech-word" data-tooltip="Modul hardware dedicat generării de baze de timp și măsurării precise a impulsurilor.">Timer</span>)<br><span class="tech-word" data-tooltip="Electrically Erasable Programmable Read-Only Memory. Memorie non-volatilă utilizată pentru salvarea pragurilor la reset.">EEPROM</span>
                    </p>
                </div>
                <div class="connection">
                    <span>Protocol I2C/TWI</span><span class="arrow">⇄</span><span><span class="tech-word" data-tooltip="Tehnică de interogare periodică (în buclă) a stării unui modul/senzor, în contrast cu folosirea întreruperilor.">Polling</span></span>
                </div>
                <div class="node">
                    <h3>SLAVE (Nano)</h3>
                    <p style="color:var(--text-muted); font-size:0.9em; margin-top:10px;">
                        [Data & Actuation]<br>Capacitive Sensors<br>Water Pumps (GPIO)
                    </p>
                </div>
            </div>
        </section>

        <section class="reveal">
            <h2 class="section-title">Database_Specii_Target</h2>
            <p style="color: var(--text-muted); margin-bottom: 30px;">Profiluri de irigare (EEPROM mapped) pentru flora de apartament.</p>
            
            <div class="flora-grid">
                <article class="flora-card">
                    <div class="flora-details">
                        <h3>Mușcată (Pelargonium) <span class="badge">ID: 0x01</span></h3>
                        <div class="param-row"><span class="param-label">Umiditate Sol:</span> <span class="param-value">[████░░░░░░] 40%</span></div>
                        <div class="param-row"><span class="param-label">Target ADC:</span> <span class="param-value">400 - 600</span></div>
                        <div class="param-row"><span class="param-label">Lumină:</span> <span class="param-value">Directă</span></div>
                        <p style="color: var(--text-muted); font-size: 0.85rem; margin-top: 15px; padding-top: 15px; border-top: 1px solid rgba(255,255,255,0.05);">
                            >_ Risc de putrezire a rădăcinilor la ADC > 800. Se irigă abundent, apoi se așteaptă uscarea completă a stratului superior al solului.
                        </p>
                    </div>
                </article>

                <article class="flora-card">
                    <div class="flora-details">
                        <h3>Trandafir (Rosa) <span class="badge">ID: 0x02</span></h3>
                        <div class="param-row"><span class="param-label">Umiditate Sol:</span> <span class="param-value">[██████▌░░░] 65%</span></div>
                        <div class="param-row"><span class="param-label">Target ADC:</span> <span class="param-value">450 - 680</span></div>
                        <div class="param-row"><span class="param-label">Lumină:</span> <span class="param-value">Directă/Intensă</span></div>
                        <p style="color: var(--text-muted); font-size: 0.85rem; margin-top: 15px; padding-top: 15px; border-top: 1px solid rgba(255,255,255,0.05);">
                            >_ Sensibil la uscăciune, preferă o udare constantă dar fără exces. Activare pompă în impulsuri PWM lente pentru a nu tasa solul.
                        </p>
                    </div>
                </article>

                <article class="flora-card">
                    <div class="flora-details">
                        <h3>Orhidee (Phalaenopsis) <span class="badge">ID: 0x03</span></h3>
                        <div class="param-row"><span class="param-label">Umiditate Sol:</span> <span class="param-value">[██░░░░░░░░] 20%</span></div>
                        <div class="param-row"><span class="param-label">Target ADC:</span> <span class="param-value">250 - 400</span></div>
                        <div class="param-row"><span class="param-label">Lumină:</span> <span class="param-value">Filtrată/Indirectă</span></div>
                        <p style="color: var(--text-muted); font-size: 0.85rem; margin-top: 15px; padding-top: 15px; border-top: 1px solid rgba(255,255,255,0.05);">
                            >_ Rădăcinile necesită aerare masivă. Sistemul aplică o peliculă minimă de apă exclusiv când substratul este complet uscat.
                        </p>
                    </div>
                </article>
            </div>
        </section>

        <section class="reveal">
            <h2 class="section-title">Bill_Of_Materials (BOM)</h2>
            <table class="bom-table">
                <thead>
                    <tr>
                        <th>Componentă</th>
                        <th>Cantitate</th>
                        <th>Rol în Sistem</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Arduino Uno (ATmega328P)</td>
                        <td>1</td>
                        <td>Unitate Centrală (Master). Gestionează decizia și HMI.</td>
                    </tr>
                    <tr>
                        <td>Arduino Nano (ATmega328P)</td>
                        <td>1</td>
                        <td>Unitate Periferică (Slave). Achiziție date și relee.</td>
                    </tr>
                    <tr>
                        <td>Senzor Umiditate Capacitiv v1.2</td>
                        <td>3</td>
                        <td>Măsoară umiditatea solului (ieșire analogică).</td>
                    </tr>
                    <tr>
                        <td>Modul Relee 5V (4 Canale)</td>
                        <td>1</td>
                        <td>Control circuit forță pentru pompele de apă.</td>
                    </tr>
                    <tr>
                        <td>Pompă Submersibilă 5V DC</td>
                        <td>3</td>
                        <td>Execută irigarea fizică per canal.</td>
                    </tr>
                    <tr>
                        <td>Display LCD 1602 + Modul I2C</td>
                        <td>1</td>
                        <td>Afișare stare sistem, alarme și nivel ADC.</td>
                    </tr>
                </tbody>
            </table>
        </section>

        <section class="reveal">
            <h2 class="section-title">Project_Roadmap</h2>
            <ul class="roadmap-list">
                <li class="done">
                    <span class="status">[✔]</span> Faza 1: Implementare Bare-Metal C, Arhitectură I2C și drivere hardware independente.
                </li>
                <li class="pending">
                    <span class="status">[ ]</span> Faza 2: Integrare modul ESP8266/ESP32 via UART pentru telemetrie MQTT în rețeaua locală.
                </li>
                <li class="pending">
                    <span class="status">[ ]</span> Faza 3: Dashboard Grafana / Node-RED pentru monitorizarea istorică a umidității per plantă.
                </li>
            </ul>
        </section>

        <section class="reveal">
            <h2 class="section-title">Build_System</h2>
            <div class="terminal-window">
                <div class="terminal-bar">
                    <div class="dot r"></div><div class="dot y"></div><div class="dot g"></div>
                </div>
                <div class="terminal-body">
<pre>
<span class="comment"># Compilare proiect pentru target (ATmega328P)</span>
<span class="prompt">victor@ubuntu:~$</span> make all
avr-gcc -Wall -Os -mmcu=atmega328p -c src/main.c -o build/main.o
avr-gcc -Wall -Os -mmcu=atmega328p -c src/i2c.c -o build/i2c.o
avr-objcopy -O ihex build/firmware.elf build/firmware.hex
<span style="color:var(--accent-green)">[SUCCESS] Build complete.</span>

<span class="comment"># Upload via avrdude</span>
<span class="prompt">victor@ubuntu:~$</span> make flash
<span class="tech-word" data-tooltip="Program din linia de comandă utilizat pentru a descărca (flash) firmware-ul precompilat direct în memoria flash a microcontrolerelor AVR.">avrdude</span> -c arduino -p atmega328p -P /dev/ttyUSB0 -b 115200 -U flash:w:build/firmware.hex
<span style="color:var(--accent-green)">[SUCCESS] Flash complete.</span>
</pre>
                </div>
            </div>
        </section>

        <footer class="reveal">
            <a href="https://github.com/razvanstefandima-11/Sistem-Automat-de-Irigare-pentru-Plante" target="_blank" class="btn">Accesare Repository GitHub_</a>
            <p style="margin-top: 20px; color: var(--text-muted); font-size: 0.8rem;">
                Dezvoltat cu pasiune pentru sistemele embedded.
            </p>
        </footer>
    </main>

    <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
    <script>
        /* Configurare Particles.js (Efect PCB) */
        particlesJS("particles-js", {
            "particles": {
                "number": { "value": 60, "density": { "enable": true, "value_area": 800 } },
                "color": { "value": "#00ff66" },
                "shape": { "type": "circle" },
                "opacity": { "value": 0.3, "random": false },
                "size": { "value": 2, "random": true },
                "line_linked": {
                    "enable": true,
                    "distance": 150,
                    "color": "#00ff66",
                    "opacity": 0.15,
                    "width": 1
                },
                "move": {
                    "enable": true,
                    "speed": 1.5,
                    "direction": "none",
                    "random": true,
                    "straight": false,
                    "out_mode": "out",
                    "bounce": false,
                }
            },
            "interactivity": {
                "detect_on": "window",
                "events": {
                    "onhover": { "enable": true, "mode": "grab" },
                    "onclick": { "enable": true, "mode": "push" },
                    "resize": true
                },
                "modes": {
                    "grab": { "distance": 180, "line_linked": { "opacity": 0.4 } },
                    "push": { "particles_nb": 3 }
                }
            },
            "retina_detect": true
        });

        /* Efect de Typewriter */
        const titleElement = document.getElementById('typewriter');
        const text = titleElement.innerText;
        titleElement.innerText = '';
        let i = 0;
        
        function typeWriter() {
            if (i < text.length) {
                titleElement.innerHTML += text.charAt(i);
                i++;
                setTimeout(typeWriter, 50); 
            }
        }
        
        window.onload = () => { setTimeout(typeWriter, 500); };

        /* Scroll Reveal */
        const revealElements = document.querySelectorAll('.reveal');
        const revealOptions = { threshold: 0.15, rootMargin: "0px 0px -50px 0px" };

        const revealOnScroll = new IntersectionObserver(function(entries, observer) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('active');
                    observer.unobserve(entry.target); 
                }
            });
        }, revealOptions);

        revealElements.forEach(el => { revealOnScroll.observe(el); });
    </script>
</body>
</html>
